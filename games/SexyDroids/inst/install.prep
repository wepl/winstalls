#version=3
#sub-dir=data
#P_MakeImages#3
      (set #AD_disk "SexyDroids")
      (P_AskDisk)
      (copyfiles
        (help @copyfiles-help)
        (source ("%s:" #AD_disk))
        (dest #dest)
	(pattern "~(devs|l|s)")
      )
